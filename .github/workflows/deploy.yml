name: Deploy to Afrihost

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: heidelbergrugbyclub.co.za
          username: heidels5d4a5
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 24
          script: |
            cd /home/heidels5d4a5/demo.heidelbergrugbyclub.co.za
            git pull origin main
            php spark migrate --all
